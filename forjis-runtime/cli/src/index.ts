/**
 * Barrel export for @forjis/cli.
 *
 * The CLI package is a thin entry point; it does not export any
 * functionality beyond what the facilitator provides. This file
 * exists to satisfy the package.json "main" field.
 */
